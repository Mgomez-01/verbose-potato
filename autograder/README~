To run: download all the submissions from canvas (shoudl give you submissions.zip)
Copy that here
mkdir submissions
cp submissions.zip submissions
cd submissions
unzip submissions
cd ..
# you are here
./autograder.py > outputFile.txt # probably easiest to save the output in a file


It will print lots of info, and then all the grades at the end.
If a student doesn't score 100, find their name in the noisy output at the top,
and copy what they got wrong into the comments on canvas
